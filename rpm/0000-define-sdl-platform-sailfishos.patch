commit f57da5db7e3b0723b34a3f370a346071172253f9
Author: nephros <nemo@pgxperiiia10>
Date:   Tue Apr 28 09:10:17 2026 +0200

    [Sailfish OS] Add SDL platform SAILFISHOS

diff --git a/CMakeLists.txt b/CMakeLists.txt
index 88728957d..72c7cf3f1 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -141,6 +141,37 @@ else()
   set(UNIX_OR_MAC_SYS OFF)
 endif()
 
+# See if we can detect Sailfish OS:
+if(LINUX)
+  if(CMAKE_VERSION VERSION_GREATER_EQUAL 3.22)
+    cmake_host_system_information(RESULT OSID QUERY DISTRIB_ID)
+    cmake_host_system_information(RESULT OSVER QUERY DISTRIB_VERSION_ID)
+    cmake_host_system_information(RESULT OSPRETTYNAME QUERY DISTRIB_PRETTY_NAME)
+    string(COMPARE EQUAL "${OSID}" "sailfishos" OS_IS_SAILFISH)
+    string(COMPARE EQUAL "${OSID}" "auroraos" OS_IS_AURORA)
+    if(OSVER VERSION_LESS 5.1.0.0)
+      set(WAYLAND_NEEDS_WL_SHELL ON)
+    else()
+      set(WAYLAND_NEEDS_WL_SHELL OFF)
+    endif()
+    message(STATUS "We are unlike! Building for ${OSPRETTYNAME}")
+    if(WAYLAND_NEEDS_WL_SHELL)
+      message(STATUS "Sailfish OS version ${OSVER} less than 5.1. This will need wl_shell support.")
+    endif()
+  else()
+    if(EXISTS "${CMAKE_SYSROOT}/etc/sailfish-release")
+      set(OS_IS_SAILFISH "true")
+      message(STATUS "We are unlike! Building for some Sailfish OS variant")
+    endif()
+  endif()
+
+  if(OS_IS_SAILFISH OR OS_IS_AURORA)
+    set(SAILFISHOS ON)
+  else()
+    set(SAILFISHOS OFF)
+  endif()
+endif()
+
 # Emscripten pthreads work, but you need to have a non-pthread fallback build
 #  for systems without support. It's not currently enough to not use
 #  pthread functions in a pthread-build; it won't start up on unsupported
@@ -267,7 +298,7 @@ if(SDL_FRAMEWORK)
   set(SDL_STATIC_AVAILABLE FALSE)
 endif()
 
-if(UNIX AND NOT ANDROID AND NOT RISCOS AND NOT SDL_FRAMEWORK)
+if(UNIX AND NOT ANDROID AND NOT RISCOS AND NOT SAILFISHOS AND NOT SDL_FRAMEWORK)
   set(SDL_RPATH_DEFAULT ON)
 else()
   set(SDL_RPATH_DEFAULT OFF)
@@ -392,6 +423,10 @@ if(EMSCRIPTEN)
   option_string(SDL_EMSCRIPTEN_PERSISTENT_PATH  "Path to mount Emscripten IDBFS at startup or '' to disable" "")
 endif()
 
+if(SAILFISHOS)
+  option(SDL_WAYLAND_WL_SHELL "Enable deprecated Wayland wl_shell support" ${WAYLAND_NEEDS_WL_SHELL})
+endif()
+
 set(SDL_VENDOR_INFO "" CACHE STRING "Vendor name and/or version to add to SDL_REVISION")
 
 cmake_dependent_option(SDL_SHARED "Build a shared version of the library" ${SDL_SHARED_DEFAULT} ${SDL_SHARED_AVAILABLE} OFF)
@@ -430,6 +465,55 @@ if (NGAGE)
   set(SDL_VIRTUAL_JOYSTICK OFF)
 endif()
 
+if (SAILFISHOS)
+  # This enables the SDL_PLATFORM_SAILFISHOS define:
+  add_compile_definitions(PRIVATE "__SAILFISHOS__")
+  set(SDL_TRAY                     OFF)
+  set(SDL_X11                      OFF)
+  set(SDL_X11_SHARED               OFF)
+  set(SDL_X11_XCURSOR              OFF)
+  set(SDL_X11_XDBE                 OFF)
+  set(SDL_X11_XFIXES               OFF)
+  set(SDL_X11_XINPUT               OFF)
+  set(SDL_X11_XRANDR               OFF)
+  set(SDL_X11_XSCRNSAVER           OFF)
+  set(SDL_X11_XSHAPE               OFF)
+  set(SDL_X11_XSYNC                OFF)
+  set(SDL_X11_XTEST                OFF)
+
+  set(SDL_DBUS                     ON)
+  if(SDL_CPU_X86)
+    set(SDL_OPENGL                 ON)
+  else()
+    set(SDL_OPENGL                 OFF)
+  endif()
+  set(SDL_OPENGLES                 ON)
+  set(SDL_PULSEAUDIO               ON)
+  set(SDL_PULSEAUDIO_SHARED        OFF)
+  set(SDL_WAYLAND                  ON)
+  set(SDL_WAYLAND_LIBDECOR         OFF)
+  set(SDL_WAYLAND_LIBDECOR_SHARED  OFF)
+  set(SDL_WAYLAND_SHARED           OFF)
+  set(SDL_FRIBIDI                  OFF)
+  set(SDL_FRIBIDI_SHARED           ON)
+
+  #set(SDL_BACKGROUNDING_SIGNAL "SDL_EVENT_WINDOW_FOCUS_LOST")
+  #set(SDL_FOREGROUNDING_SIGNAL "SDL_EVENT_WINDOW_FOCUS_GAINED")
+
+  if(CMAKE_VERSION VERSION_GREATER_EQUAL 3.22)
+    cmake_host_system_information(RESULT OSNAME QUERY DISTRIB_NAME)
+    set(SDL_VENDOR_INFO "${OSNAME}")
+  else()
+    if(DEFINED ENV{NAME})
+      set(SDL_VENDOR_INFO "$ENV{NAME}")
+    endif()
+  endif()
+
+  if(SDL_WAYLAND_WL_SHELL)
+    sdl_compile_definitions(PRIVATE "SDL_WAYLAND_WL_SHELL")
+  endif()
+endif()
+
 if(NOT (SDL_SHARED OR SDL_STATIC))
   message(FATAL_ERROR "SDL_SHARED and SDL_STATIC cannot both be disabled")
 endif()
@@ -1948,6 +2032,14 @@ elseif(UNIX AND NOT APPLE AND NOT RISCOS AND NOT HAIKU)
           sdl_include_directories(PRIVATE SYSTEM ${DBUS_INCLUDE_DIRS})
           # Fcitx need only dbus.
           set(HAVE_FCITX TRUE)
+          if (SAILFISHOS)
+            pkg_search_module(MALIIT maliit-glib)
+            if(MALIIT_FOUND)
+              set(HAVE_MALIIT TRUE)
+              message(CONFIGURE_LOG "FIXME: Maliit IME enabled, but not implemented!")
+              message(STATUS "FIXME: Maliit IME enabled, but not implemented!")
+            endif()
+          endif()
           set(HAVE_DBUS TRUE)
         endif()
       endif()
@@ -1962,7 +2054,7 @@ elseif(UNIX AND NOT APPLE AND NOT RISCOS AND NOT HAIKU)
         endif()
       endif()
 
-      if (HAVE_IBUS_IBUS_H OR HAVE_FCITX)
+      if (HAVE_IBUS_IBUS_H OR HAVE_FCITX OR HAVE_MALIIT)
         set(SDL_USE_IME 1)
       endif()
 
@@ -2115,7 +2207,7 @@ elseif(UNIX AND NOT APPLE AND NOT RISCOS AND NOT HAIKU)
 
   set(SDL_STORAGE_GENERIC 1)
   sdl_glob_sources("${SDL3_SOURCE_DIR}/src/storage/generic/*.c")
-  if(LINUX)
+  if(LINUX AND NOT SAILFISHOS)
     set(SDL_STORAGE_STEAM 1)
     sdl_glob_sources(
       "${SDL3_SOURCE_DIR}/src/storage/steam/*.c"
@@ -4352,5 +4444,16 @@ endif()
 ##### Fix Objective C builds #####
 string(APPEND CMAKE_OBJC_FLAGS " ${CMAKE_C_FLAGS}")
 
+if(SDL_WAYLAND_WL_SHELL)
+  message(CONFIGURE_LOG "Building with wl_shell support.")
+  message(WARNING "Building with wl_shell support.\n"
+          "This may break ABI/API compatability in places.\n"
+          "Note that this is not supported by libsdl. Do not file upstream bug reports about this."
+  )
+else()
+  message(WARNING "Without built-in wl_shell you will need to handle it in any applications manually, or use a Compositor with xdg_shell support.")
+endif()
+
+
 SDL_PrintSummary()
 debug_show_sdl_deps()
diff --git a/docs/README-sailfishos.md b/docs/README-sailfishos.md
new file mode 100644
index 000000000..fd4dc7d80
--- /dev/null
+++ b/docs/README-sailfishos.md
@@ -0,0 +1,38 @@
+Sailfish OS
+=======
+SDL port for the Sailfish OS mobile operating system
+
+
+Building
+--------
+Sailfish OS is Linux, so the build instructions for Linux apply.
+
+We do not have X11 support so this is disabled.
+
+Also, as Sailfish OS does not have some concepts common on Dessktop Linux,
+things like Tray (`SDL_TRAY`) and Dialog (`SDL_DIALOG`) are disabled for now.
+
+You may want to toggle `wl_shell` support by toggling the `SDL_WAYLAND_WL_SHELL` cmake option.
+
+Other options:
+
+```sh
+cmake -S. -Bbuild -DSDL_RENDER_VULKAN=ON -DSDL_RENDER_GPU=ON -DSDL_OPENVR=ON
+cmake --build build
+cmake --install build
+```
+
+
+Notes
+-----
+* The port includes an EXPERIMENTAL implementation of the deprecated `wl_shell`
+  wayland protocol.  May be enabled by configuring with `-DSDL_WAYLAND_WL_SHELL=ON`.
+* experimental support for droidmedia-based cameras. This driver may be
+  required to be explicitly requested by setting the hint
+  [`SDL_HINT_CAMERA_DRIVER`](https://wiki.libsdl.org/SDL3/SDL_HINT_CAMERA_DRIVER),
+  or the variable `SDL_CAMERA_DRIVER=droidcamera`. Otherwise the Linux default
+  (v4l2) is selected.
+* OpenURL: Urls will be opened using the Lipstick OpenUrl method
+* experimental/WIP: Support for sensorfw-based sensors.
+* experimental/WIP: Support for display bounds (rounded corners/camera notches)
+* Missing features: Maliit IME. Virtual/Touch Keyboard input may or may not work depending on the application.
diff --git a/include/SDL3/SDL_platform_defines.h b/include/SDL3/SDL_platform_defines.h
index 79631491d..b43372797 100644
--- a/include/SDL3/SDL_platform_defines.h
+++ b/include/SDL3/SDL_platform_defines.h
@@ -96,9 +96,15 @@
  * Note that Android, although ostensibly a Linux-based system, will not
  * define this. It defines SDL_PLATFORM_ANDROID instead.
  *
+ * Sailfish OS will both define this, and its own SDL_PLATFORM_SAILFISHOS
+ *
  * \since This macro is available since SDL 3.2.0.
  */
 #define SDL_PLATFORM_LINUX 1
+/* Sailfish OS is Linux but not only Linux */
+#if defined(__SAILFISHOS__)
+#define SDL_PLATFORM_SAILFISHOS 1
+#endif
 #endif
 
 #if defined(ANDROID) || defined(__ANDROID__)
